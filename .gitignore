# Go
/bin/
/build/
*.out
coverage.out

# Node
node_modules/
.next/
out/
apps/web/tsconfig.tsbuildinfo

# Editör / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env / Secrets
.env
.env.local
.env.*.local
.secrets
.secrets/
*.pem
*.key

# linter/editor backup files (cannot be deleted from Windows mount)
*.go.[0-9]*
*.go.[0-9]*[0-9]*


# stray go build outputs (artifact when running `go build` w/o -o)
/api
/worker
*-go-tmp-*

# lokal smoke harness artifacts (binary + log dosyaları, asla commit edilmez)
.smoke_*

